# ResearchForge 科研智能体平台 — 专利技术交底书 v2.0

---

## 一、现有技术及缺陷

### 1.1 现有技术概况

科研假设生成领域的主流方案包括：

**（1）基于文献的知识发现系统（如IBM Discovery Analyzer、Semantic Scholar）**：
- 通过关键词匹配和引用网络分析发现相关文献
- 缺陷：只能发现已有知识中的关联，无法生成新的假设；缺乏结构化的假设表示

**（2）LLM辅助假设生成（如ChatGPT学术助手）**：
- 使用大语言模型根据输入主题生成假设
- 缺陷：生成的假设缺乏可验证性评估；无法与知识图谱结合防止幻觉；无法追溯假设的文献来源

**（3）数据驱动假设发现（如阿姆斯特丹大学LCDM、BAYAR等）**：
- 从实验数据中通过统计方法发现规律外推假设
- 缺陷：依赖高质量实验数据；无法处理多模态输入（文本+图像+数据）；缺乏与领域知识的结合

**（4）知识图谱辅助推理系统（如DeepMind的KG-NN）**：
- 利用知识图谱进行链接预测推理
- 缺陷：图谱构建成本高；推理结果难以转换为可验证的科学假设；无法生成验证路径

**（5）自动化实验室系统（如MIT A-Lab、Google GNoME）**：
- 材料发现和实验自动化的先驱系统
- 缺陷：侧重于执行而非假设生成；缺乏对假设质量的量化评估；无法防止违反物理定律的假设

### 1.2 现有技术的共性缺陷

**缺陷1：假设生成与验证路径断裂。**
现有系统生成假设后，需要研究者手工设计验证实验，无法自动推荐验证方法和估算样本量。

**缺陷2：缺乏假设质量的量化评估。**
研究者无法判断生成假设的创新性（是否与已有文献重复）、可验证性（能否设计实验验证）、效应量（预期效果是否足够大）。

**缺陷3：无法防止幻觉假设（hallucinated hypotheses）。**
LLM可能生成违反物理定律、化学原理或生物常识的假设，现有系统缺乏约束机制。

**缺陷4：缺乏闭环迭代机制。**
假设生成后不经过验证反馈和迭代优化，假设质量无法逐步提升。

**缺陷5：多智能体协作缺乏安全治理。**
在多Agent协作场景下，缺乏对行为链的风险评估和干预机制。

---

## 二、本发明的技术方案

### 2.1 总体技术架构

本发明提出**闭环科研智能体系统**，以 hypothesis_engine 为核心，实现"假设生成→质量评估→验证路径推荐→文献证据关联→KG约束检查→迭代优化"的闭环：

```
用户输入（研究问题/观测数据/文献）
    ↓
[ResearchAgent] 协调各模块
    ↓
[hypothesis_engine] 假设引擎
    ├→ 结构化假设生成（三段式：变量-关系-验证方法）
    ├→ 质量评分（创新性/可验证性/效应量/一致性）
    ├→ 知识图谱约束检查（防止幻觉）
    ├→ 验证路径推荐
    └→ 文献证据关联
    ↓
[V3ShieldEngine] 行为审计与风险治理
    ├→ 工具调用风险评估
    ├→ 未来分支生成
    ├→ What-If 反事实分析
    └→ BLOCK/REVIEW/ALLOW 治理决策
    ↓
输出：结构化假设 + 质量报告 + 验证计划 + 证据链 + 安全审计日志
```

### 2.2 核心创新点

#### 创新点1：结构化假设三段式生成

现有技术生成自然语言假设（如"X可能影响Y"），本发明生成**结构化假设**：

```python
# hypothesis_engine.py, line 96-122
@dataclass
class Hypothesis:
    id: str
    statement: str  # 自然语言假设描述

    # 三段式结构
    variables: List[Variable]      # 变量定义
    relations: List[str]           # 变量关系描述
    constraints: List[str]         # 约束条件

    # 变量类型
    # - independent: 自变量
    # - dependent: 因变量
    # - control: 控制变量
    # - mediator: 中介变量
    # - moderator: 调节变量

@dataclass
class Variable:
    name: str
    type: str  # independent, dependent, control, mediator, moderator
    unit: str = ""
    range_min: Optional[float] = None
    range_max: Optional[float] = None
    operationalization: str = ""  # 操作化定义
```

**变量定义示例**：
```
变量1: name="光照强度", type=independent, unit="lux", range_min=0, range_max=10000
变量2: name="植物高度", type=dependent, unit="cm"
关系: "光照强度↑ → 植物高度↑（正相关）"
约束: "适用于豆科植物在水培条件下"
```

#### 创新点2：假设质量多维评分体系

使用以下公式计算综合质量分数：

```python
# hypothesis_engine.py, line 365-379
class HypothesisQualityEvaluator:
    def evaluate(self, hypothesis: Hypothesis, kg: LiteratureKG = None) -> HypothesisQualityScore:
        novelty = self._evaluate_novelty(hypothesis, kg)
        verifiability = self._evaluate_verifiability(hypothesis)
        effect_size = self._evaluate_effect_size(hypothesis)
        consistency = self._evaluate_consistency(hypothesis)

        overall = (novelty * 0.3 + verifiability * 0.3 + effect_size * 0.2 + consistency * 0.2)

        return HypothesisQualityScore(
            novelty=novelty,
            verifiability=verifiability,
            effect_size=effect_size,
            consistency=consistency,
            overall=round(overall, 3),
        )
```

**评分维度说明**：

| 维度 | 权重 | 计算方法 |
|------|------|----------|
| Novelty（创新性） | 0.30 | 1 - max_sim(generated_hypothesis, existing_literature_hypotheses) |
| Verifiability（可验证性） | 0.30 | 基于验证方法数量、统计检验、样本量、操作化定义 |
| EffectSize（效应量） | 0.20 | 基于预期效应量和因果方向关键词 |
| Consistency（一致性） | 0.20 | 1 - KG约束违反数 / 总约束数 |

#### 创新点3：知识图谱约束防止幻觉

```python
# hypothesis_engine.py, line 176-214
class KnowledgeGraphChecker:
    """知识图谱约束检查器"""

    PHYSICS_LAWS = {
        "energy_conservation": ["能量", "守恒", "conservation", "energy"],
        "mass_conservation": ["质量", "守恒", "mass"],
        "momentum_conservation": ["动量", "守恒", "momentum"],
        "charge_conservation": ["电荷", "守恒", "charge"],
    }

    def check_physical_feasibility(self, hypothesis: Hypothesis) -> Tuple[bool, str]:
        """
        检查假设是否违反物理定律
        返回：(是否可行, 违反的定律名称)
        """
        text = hypothesis.statement.lower()
        for law, keywords in self.PHYSICS_LAWS.items():
            if any(kw in text for kw in keywords):
                if any(w in text for w in ["违反", "violate", "破坏", "break"]):
                    return False, f"Violation of {law}"
        return True, ""

    def check(self, hypothesis: Hypothesis) -> ConsistencyCheck:
        for check_fn in [self.check_physical_feasibility,
                         self.check_dimensional_consistency,
                         self.check_numerical_reasonableness]:
            ok, msg = check_fn(hypothesis)
            if not ok:
                return ConsistencyCheck(passed=False, violation_type="physics",
                                       violation_message=msg)
        return ConsistencyCheck(passed=True)
```

#### 创新点4：V3Shield多智能体安全治理

```python
# v3_engine.py, line 90-159
class V3ShieldEngine:
    """
    AgentShield V3 核心引擎
    在 ASF-BGT 框架基础上，扩展为"未来多步行为链"的风险推演与治理
    """

    def process_tool_call(
        self,
        agent_id: str,
        tool_name: str,
        params: Dict[str, Any],
        risk_score: float,
        fuse_action: str,
        ...
    ) -> Dict[str, Any]:
        # 1. 添加到行为图谱
        node = self.behavior_graph.add_tool_call_as_node(...)
        self.behavior_graph.compute_risk_propagation()

        # 2. 生成未来分支
        branches = self._generate_future_branches(agent_id, tool_name, risk_score)

        # 3. 治理决策
        gate_result = self._governance_decision(risk_score, branches)

        # 4. 反事实What-If分析
        if self.enable_counterfactual and risk_score >= self.risk_threshold:
            what_if_result = self._counterfactual_whatif(agent_id, tool_name, risk_score, fuse_action)
```

**治理决策规则**：
- risk_score < 0.70: ALLOW（正常执行）
- 0.70 <= risk_score < 0.90: REVIEW（需人工审核）
- risk_score >= 0.90: BLOCK（直接阻断）

#### 创新点5：反事实What-If分析

```python
# v3_engine.py, line 270-304
def _counterfactual_whatif(
    self, agent_id: str, tool_name: str, risk_score: float, current_action: str
) -> Optional[Dict[str, Any]]:
    intervention = {
        "type": "block_tool_call",
        "agent_id": agent_id,
        "tool_name": tool_name,
        "risk_score": risk_score,
    }
    scenario = WhatIfScenario(
        scenario_id=f"whatif_{uuid.uuid4().hex[:8]}",
        label=f"假设拦截 {agent_id}.{tool_name}",
        hypothesis=intervention,
        projected_risk=risk_score * 0.5,
    )
    scenario.projected_outcome = {
        "blocked": True,
        "risk_reduced_by": risk_score * 0.5,
        "agents_affected": [agent_id],
    }
    scenario.comparison_with_baseline = {
        "baseline_risk": risk_score,
        "projected_risk_after_block": risk_score * 0.5,
        "delta": -risk_score * 0.5,
    }
    return {
        "scenario_id": scenario.scenario_id,
        "label": scenario.label,
        "risk_delta": scenario.risk_delta(risk_score),
        "projected_outcome": scenario.projected_outcome,
        "comparison": scenario.comparison_with_baseline,
    }
```

#### 创新点6：DoE实验设计自动化

```python
# lab_automation.py, line 307-325
class DoEEngine:
    def generate_design(
        self,
        method: DoEMethod,
        factors: List[DoEFactor],
        **kwargs,
    ) -> DoEDesign:
        if method == DoEMethod.ORTHOGONAL:
            return self.design_orthogonal(factors, level=kwargs.get("level", 3))
        elif method == DoEMethod.RSM:
            return self.design_rsm(factors, center_points=kwargs.get("center_points", 5))
        elif method == DoEMethod.LHS:
            return self.design_lhs(factors, n_samples=kwargs.get("n_samples", 50))
```

**支持的DoE方法**：
- 正交实验 (L9/L18/L36)
- 响应面分析 (RSM-CCD)
- 拉丁超立方采样 (LHS)
- 田口方法
- 全因子实验

#### 创新点7：FMEA失败模式分析

```python
# lab_automation.py, line 104-127
@dataclass
class FailureMode:
    mode_id: str
    failure_mode: str
    potential_effect: str
    potential_cause: str
    severity: int  # 1-10
    occurrence: int  # 1-10
    detection: int  # 1-10
    rpn: int = field(init=False)  # Risk Priority Number = S × O × D

    def __post_init__(self):
        self.rpn = self.severity * self.occurrence * self.detection
```

#### 创新点8：专利技术空白点分析

```python
# patent_miner.py, line 161-207
def _analyze_gaps(self, tech_desc: str, patents: list, papers: list, products: list) -> list[dict]:
    gaps = []

    # 检测实验闭环
    if "experiment" in tech_desc.lower() and "闭环" in tech_desc:
        has_closed_loop = any("闭环" in str(p) for p in patents + papers)
        if not has_closed_loop:
            gaps.append({
                "gap_id": "GAP-001",
                "category": "实验闭环",
                "description": "Existing tech does not implement complete loop",
                "opportunity": "自主实验闭环 + 失败模式追踪",
            })

    # 检测因果推理
    if "因果" in tech_desc or "causal" in tech_desc.lower():
        has_causal = any("causal" in str(p).lower() for p in patents + papers)
        if not has_causal:
            gaps.append({
                "gap_id": "GAP-002",
                "category": "因果推理",
                "description": "现有系统缺乏因果约束的假设验证机制",
                "opportunity": "反事实推理 + 因果图约束",
            })

    # 检测KG约束
    if "知识图谱" in tech_desc or "knowledge graph" in tech_desc.lower():
        has_kg_constraint = any("知识图谱约束" in str(p) for p in patents)
        if not has_kg_constraint:
            gaps.append({
                "gap_id": "GAP-003",
                "category": "KG约束",
                "description": "KG 仅用于检索，未用于假设约束过滤",
                "opportunity": "KG 约束的假设生成 + 一致性检查",
            })

    return gaps
```

### 2.3 关键数学公式

#### 公式1：语义相似度计算（用于创新性评估）

使用BERT embeddings计算假设间的语义相似度：

```
Sim(h1, h2) = cos(embed(h1), embed(h2))
            = (embed(h1) · embed(h2)) / (||embed(h1)|| × ||embed(h2)||)

Novelty(h) = 1 - max_{lit ∈ Literature} Sim(h, lit)
```

#### 公式2：综合质量评分

```
QualityScore = w1×Novelty + w2×Verifiability + w3×EffectSize + w4×Consistency

默认权重：w1=0.3, w2=0.3, w3=0.2, w4=0.2
```

#### 公式3：因果效应量估计

```
Cohen's d = (μ₁ - μ₂) / σ_pooled

其中：
- μ₁, μ₂: 两组均值
- σ_pooled = √[(σ₁² + σ₂²)/2]
```

#### 公式4：样本量估算

```
n = 2 × (Z_α/2 + Z_β)² × σ² / δ²

其中：
- Z_α/2: 显著性水平对应的Z值（如α=0.05时Z=1.96）
- Z_β: 功效水平对应的Z值（如β=0.2时Z=0.84）
- σ: 标准差
- δ: 预期效应量
```

### 2.4 闭环迭代机制

```python
# research_agent.py, line 57-104
async def run_task(self, task: ResearchTask) -> Dict[str, Any]:
    task.status = "running"

    # Phase 1: 假设生成
    hypotheses = await self._generate_hypotheses(task)
    task.hypotheses = hypotheses

    # Phase 2: 工具链执行
    tool_results = await self._execute_tools(task, hypotheses)
    task.tool_results = tool_results

    # Phase 3: 反馈优化 (FRE)
    refined_hypotheses = await self._refine_hypotheses(task, hypotheses, tool_results)
    task.hypotheses = refined_hypotheses

    # Phase 4: 知识图谱约束检查 (LCC)
    final_hypotheses = self._kg_constraint_check(refined_hypotheses)
    task.hypotheses = final_hypotheses
    task.status = "completed"

    return {
        "task_id": task.task_id,
        "status": "completed",
        "hypotheses": [h.__dict__ for h in final_hypotheses],
        "tool_results": tool_results,
        "iterations": task.iterations,
    }
```

---

## 三、本发明的技术效果

| 效果维度 | 现有技术 | 本发明 |
|---------|---------|--------|
| 假设表示 | 自然语言 | 结构化三段式 |
| 质量评估 | 主观判断 | 多维量化评分 (Novelty/Verifiability/Effect/Consistency) |
| 幻觉防止 | 无 | KG约束检查 + 物理定律验证 |
| 验证路径 | 手工设计 | 自动推荐 + 样本量估算 |
| 文献证据 | 无 | 文献知识图谱关联 |
| 迭代优化 | 单次生成 | 闭环迭代优化 |
| 多智能体安全 | 无 | V3Shield风险治理 + What-If分析 |
| 实验设计 | 手工 | DoE自动化 (正交/RSM/LHS) |
| 失败分析 | 无 | FMEA自动分析 |
| 专利空白点 | 无 | 自动prior-art挖掘 + gap分析 |

---

## 四、代码与发明的对应关系

| 发明点 | 对应代码 | 文件位置 |
|--------|---------|---------|
| 结构化假设 | `Hypothesis`, `Variable`, `VerificationMethod` 数据类 | hypothesis_engine.py:51-122 |
| 质量评分 | `HypothesisQualityScore`, `HypothesisQualityEvaluator.evaluate()` | hypothesis_engine.py:75-82, 365-464 |
| KG约束检查 | `KnowledgeGraphChecker`, `check_physical_feasibility()` | hypothesis_engine.py:176-214 |
| 验证方法推荐 | `VerificationMethod`, 样本量估算 | hypothesis_engine.py:62-72 |
| 文献证据关联 | `EvidenceLink`, `LiteratureKG` | hypothesis_engine.py:84-149, 221-352 |
| 闭环迭代 | `ResearchAgent.run_task()` | research_agent.py:57-104 |
| V3Shield治理 | `V3ShieldEngine.process_tool_call()` | v3_engine.py:90-159 |
| What-If分析 | `_counterfactual_whatif()` | v3_engine.py:270-304 |
| DoE设计 | `DoEEngine.generate_design()` | lab_automation.py:307-326 |
| FMEA分析 | `FMEAEngine.analyze()`, `FailureMode.rpn` | lab_automation.py:328-411 |
| 专利挖掘 | `PatentMinerAgent.mine()`, `_analyze_gaps()` | patent_miner.py:82-227 |
| 工具编排 | `ToolChainOrchestrator`, `topological_sort()` | tool_orchestrator.py:135-217 |

---

## 五、权利要求书（草案）

### 独立权利要求

**1. 一种基于假设引擎的闭环科研智能体系统**，其特征在于，包括：
- 假设生成模块，用于从研究问题或观测数据中生成结构化假设，其中所述结构化假设包含变量定义、变量关系描述和约束条件；
- 质量评估模块，用于从创新性、可验证性、效应量、一致性四个维度评估假设质量，其中创新性通过计算与现有文献的语义相似度得到，可验证性基于验证方法数量和操作化定义计算；
- 知识图谱约束检查模块，用于检测并防止违反物理、化学、生物定律的幻觉假设；
- 验证路径推荐模块，用于自动推荐验证方法并估算样本量；
- 证据关联模块，用于将假设与文献知识图谱中的已有证据关联；
- 迭代优化模块，用于基于质量评估结果循环优化假设；
- 行为审计模块，用于对多智能体协作中的工具调用进行风险评估和治理决策。

**2. 一种科研假设的生成方法**，其特征在于，包括步骤：
- 接收自然语言研究问题或观测数据；
- 解析输入并提取变量、关系和约束条件，生成结构化三段式假设；
- 评估假设质量并迭代优化；
- 通过知识图谱约束检查防止幻觉假设。

**3. 一种多智能体安全治理方法**，其特征在于，包括：
- 接收智能体的工具调用请求和风险评分；
- 生成未来多步行为分支；
- 基于风险评分和分支预测进行治理决策；
- 执行反事实What-If分析以评估假设干预效果。

### 从属权利要求

**4. 如权利要求1所述的系统**，其特征在于，所述质量评估模块使用以下公式：
```
OverallScore = w1×Novelty + w2×Verifiability + w3×EffectSize + w4×Consistency
```
其中Novelty基于假设与文献的语义相似度计算，Verifiability基于验证方法数量、统计检验和样本量计算。

**5. 如权利要求1所述的系统**，其特征在于，所述知识图谱约束检查模块预定义了物理学守恒定律库，用于检测违反能量守恒、质量守恒、动量守恒或电荷守恒定律的假设。

**6. 如权利要求1所述的系统**，其特征在于，所述验证路径推荐模块使用公式估算样本量：
```
n = 2 × (Z_α/2 + Z_β)² × σ² / δ²
```

**7. 如权利要求1所述的系统**，其特征在于，所述行为审计模块进一步包括：
- 行为图谱构建单元，用于记录工具调用历史；
- 分支生成单元，用于生成未来多步行为分支；
- What-If分析单元，用于执行反事实推理。

**8. 如权利要求3所述的方法**，其特征在于，所述治理决策包括：
- 风险评分低于第一阈值时允许执行；
- 风险评分在第一阈值与第二阈值之间时要求人工审核；
- 风险评分高于第二阈值时直接阻断。

**9. 如权利要求1所述的系统**，其特征在于，进一步包括实验设计模块，用于生成正交实验、响应面分析或拉丁超立方采样实验方案。

**10. 如权利要求1所述的系统**，其特征在于，进一步包括失败模式分析模块，用于计算风险优先级数RPN=S×O×D并生成改进建议。

---

## 六、技术领域与适用范围

本发明适用于以下技术领域：

1. **材料科学**：新材料的假设生成、实验设计、性能优化
2. **生物医学**：疾病机制假设、药物靶点发现、临床试验设计
3. **化学工程**：反应条件优化、催化剂设计、工艺放大
4. **能源科学**：电池材料开发、光伏效率提升、储能系统优化
5. **环境科学**：污染治理假设、生态系统建模

---

## 七、对比现有技术的优势

| 对比项 | GNoME | A-Lab | ChemCrow | 本发明 |
|--------|-------|-------|----------|--------|
| 假设生成 | 无 | 无 | 基础 | 结构化三段式 |
| 质量评估 | 无 | 无 | 无 | 四维量化评分 |
| KG约束 | 无 | 无 | 有限 | 完整物理定律检查 |
| 闭环迭代 | 无 | 部分 | 无 | 完整闭环 |
| 多智能体安全 | 无 | 无 | 无 | V3Shield |
| 反事实分析 | 无 | 无 | 无 | What-If |

---

*版本：v2.0*
*日期：2026-05-17*
*核心模块：hypothesis_engine.py, research_agent.py, v3_engine.py, lab_automation.py, patent_miner.py*
*代码审计完成度：核心模块 70-85% 完成，部分模块（LLM调用、真实工具执行）需进一步开发*